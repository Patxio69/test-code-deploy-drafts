#Files to fuck up with 
#you.user = 
arrayever = [10, 10, 20, 33, 00] # we fucked up this code, gotta debug
for what in arrayever:
    print(what)
print(arrayever)    
def whatever(arg):
    
    #edit the code 
    #make sure to add comments
    def appender():
        inputer = input("commend")
        if int(inputer) > 0:
            arrayever.append(inputer)
        elif inputer == 0:
            arrayever.remove[0:3]
        elif len.arrayever == 0:
            arrayever.append("user didn't do any shit")
            your_response = input("do you want to y or n?")
            if your_response == "y":
                arrayever.remove()
                return "everything removed"
                arrayever.append[0:3] = [0, 0, 0, 0]
                if arrayever == [0,0,0,0]:
                    return "default values loaded"
            
            elif your_response == "m":
                pass
                print("ok, got it") 
        
        else:
            return "you don't know what you're doing"    
    
    for cyclers in arrayever:
        if cyclers > 4:
             arrayever.append(100)
             sum(arrayever)
saved_array = []
start_with = "you have" + str(saved_array)
array = []
print("how many interations?")
val = input()

def value():    #value provider for inter_n()
    userinput = input("write your value")
    value = ''
    if int(userinput) > 0:
        value = str(userinput)
    else:
        (value) = 0
    
    return value

def inter_n():    #test block 1
    n = 0
    while True:
        print("enter value")
        count = input(int())
        array.append(count)  #modify so if a string value is entred, it checkes
        if str(count) == "done":
            break  # after that the array is loaded
    print("enter how many times")
    count_entry = input(int())

    if int(count_entry) > 0:
        for looper in range(count_entry):
            num = 0
            values = value
            while num == 10:
                num = num + values()

    elif int(count_entry) == 0:
        return "write somethign"

    n = len(array)
    return n


def user_response():
       default_messge = "say either 'y' or 'n'"
       user_respose_var = input("#user respose here")
       if user_respose_var == 'yes':
           for user_loop in range(n):
               res_postul = ["your number of bs are" + user_loop]
               return res_postul
        else:
            pass


fuk_args1 = whatever(arg = inputuser)



